# 변경 내역

이 프로그램이 어떻게 바뀌어 왔는지 적습니다.
버전은 `큰자리.중간자리.작은자리` 로 매깁니다.

| 자리 | 언제 올리나 |
|---|---|
| 큰자리 | 쓰던 방식이 달라져서 안내가 필요할 때 |
| 중간자리 | 기능이 늘거나 화면이 눈에 띄게 바뀔 때 |
| 작은자리 | 잘못된 것을 고쳤을 때 |

서버에 올린 뒤 `http://서버주소:8080/api/health` 를 열면 지금 돌고 있는
버전을 확인할 수 있습니다.

---

## 0.5.0 — 2026-09-08

### ⚠️ 올릴 때 할 일

**회의록 표에 칸이 늘었습니다.** 서버에서 받은 뒤 꼭 돌려 주세요.
안 돌리면 회의록 화면이 열리지 않습니다.

```
cd backend
..\.venv\Scripts\python.exe -m alembic upgrade head
```

이미 저장해 둔 회의록은 그대로 있고, 새 칸만 빈 값으로 채워집니다.

### 새로 생긴 것

- **회의 시간.** 일시 옆에 시작·종료 시각을 적습니다. 감사에서 회의
  시간대를 확인하는 일이 있어(식대·초과근무와 겹치는지) 문서에 남깁니다.
  종료는 비워 둬도 됩니다 — 언제 끝났는지 모른 채로 적는 일이 흔한데,
  억지로 채우게 하면 없는 숫자를 지어내게 됩니다.
- **작성자.** 장소 옆에 적습니다. 그동안 문서에서 빈칸으로 나가
  손으로 적어야 했습니다.
- **요청 및 예정 사항.** 회의내용에서 이어지는 할 일을 줄 단위로 적습니다.
  [AI 생성] 을 누르면 **지금 화면에 적힌 회의내용을 바탕으로** 씁니다.
  회의내용을 손본 뒤 다시 누르면 고친 내용에 맞춰 다시 나옵니다.

  회의내용 초안을 만들 때도 요청 사항이 함께 나옵니다. 다만 이미 적어 둔
  것이 있으면 덮지 않습니다.

### 고친 것

- 회의록 문서의 **시간 · 작성자 · 요청 및 예정 사항** 세 칸이 빈 채로
  나가던 것을 채웁니다. 양식에는 있는데 받는 곳이 없었습니다.
- 공고 화면의 **보기 전환 단추에서 지금 고른 쪽이 표시되지 않던 것**.
  없는 색 이름을 쓰고 있었습니다. 달력 확대 단추도 같은 문제였습니다.
- 회의록 저장 단추의 자물쇠 표시가 글자와 따로 한 줄을 차지하던 것.

### 달라진 것

- 회의록 화면에서 **이모지를 걷어냈습니다.** 다른 화면과 같이 선으로 그린
  아이콘만 씁니다.
- 회의록·검토중 화면의 색을 나머지 화면과 같은 색 이름으로 맞췄습니다.
  직접 적어 둔 색은 어두운 화면에서 바뀌지 않아 그 화면만 밝게 떠 보였습니다.

---

## 0.4.0 — 2026-09-08

### ⚠️ 올릴 때 할 일

**표가 늘었습니다.** 서버에서 받은 뒤 아래를 꼭 돌려 주세요.

```
cd backend
..\.venv\Scripts\python.exe -m alembic upgrade head
```

**라이브러리가 늘었습니다.**

```
.venv\Scripts\pip install -r requirements.txt
```

**AI 초안을 쓰려면** 서버의 `.env` 에 Google Gemini 키를 넣습니다.
`ai.google.dev` 에서 받습니다. 비워 두면 AI 단추만 꺼지고 회의록은
손으로 써서 그대로 씁니다.

```
GEMINI_API_KEY=받은키
```

서버가 `generativelanguage.googleapis.com` 에 나갈 수 있어야 합니다.

### 새로 생긴 것

- **회의록 자동 생성기.** 사업비 지출 증빙(감사·정산)에 쓰는 회의록을
  사업 안에서 만들고 관리합니다. 사업 대시보드에 [현황 | 회의록] 탭이
  생겼습니다. 회의록은 그 사업 예산을 쓴 근거라, 사업 밖에 두면 '어느 사업
  회의비인지' 가 사라져 증빙 구실을 못 합니다.

  - 회의명·일시·장소·참석자·회의내용·금액을 적습니다
  - 참석자는 칩으로 넣고 뺍니다 (쉼표로 여러 명 한 번에)
  - 회의내용은 줄 단위입니다. 줄을 눌러 그 자리에서 고칩니다
  - **AI 초안** — 회의명과 메모로 회의내용 6~8줄을 만듭니다. 메모에 적은
    낱말을 녹여 쓰고 명사형으로 끝맺습니다. 만든 내용은 저장되지 않고
    화면에만 올라오며, 사람이 고쳐 저장해야 남습니다
  - 다시 생성하면 한 번 묻고 **이전 내용을 아래에 남깁니다.** 통째로
    되돌리거나 필요한 줄만 가져올 수 있습니다
  - **회의명 다듬기** — 수정안 4개를 칩으로 제시합니다
  - **사진** — 끌어다 놓거나 눌러서 붙입니다. 문서 뒤에 원본 비율로 들어갑니다
  - **금액** — 천 단위 콤마가 붙고, 저장될 파일 이름이 실시간으로 보입니다
  - **docx 로 내려받습니다.** 한글(HWP)에서도 열립니다
  - 회의명과 내용이 없으면 저장 단추가 잠깁니다 (빈 회의록은 증빙이 안 됩니다)

---

## 0.3.0 — 2026-09-03

### ⚠️ 올릴 때 할 일

**표 구조가 바뀌었습니다.** 서버에서 받은 뒤 아래를 꼭 돌려 주세요.

```
cd backend
..\.venv\Scripts\python.exe -m alembic upgrade head
```

### 달라진 것

- **예산 비목의 배정액을 국고보조금과 자기부담금으로 나눠 입력합니다.**
  사업비가 두 몫으로 내려오고 비목마다 비율이 다르기 때문입니다.
  화면에 보이는 배정액·집행률·잔액은 **예나 지금이나 두 값을 더한 합계**만
  씁니다. 나눈 값은 사업을 고칠 때 입력칸을 다시 채우는 데 씁니다.
  등록 화면 아래에 '국고 · 자부담 = 배정 합계' 가 나와, 채우면서 총액이
  맞는지 바로 확인할 수 있습니다.

  이미 등록된 비목은 나눈 정보가 없어 **전액을 국고보조금으로** 넣습니다.
  자기부담금이 있는 비목은 사업 수정에서 고쳐 주세요.

---

## 0.2.0 — 2026-09-03

처음 서버에 올리기 위해 준비하고, 화면을 실제로 써 보며 다듬은 판입니다.

### ⚠️ 올릴 때 할 일

**표 구조가 바뀌었습니다.** 서버에서 받은 뒤 아래를 꼭 돌려 주세요.
빠뜨리면 '그런 컬럼이 없다' 는 오류가 납니다.

```
cd backend
..\.venv\Scripts\python.exe -m alembic upgrade head
```

포트를 8080 으로 쓰기로 했습니다. WinSW 설정(`--port`)과 방화벽 규칙도
함께 고쳐 주세요. 자세한 것은 [docs/설치방법.txt](docs/설치방법.txt) 를 보세요.

### 새로 생긴 것

- 공고를 **목록으로도** 볼 수 있습니다. 카드와 목록을 아이콘으로 오갑니다.
  고른 방식은 다음에 열 때도 유지됩니다.
- **할 일을 달력에서 바로 적습니다.** 입력칸을 따로 두지 않고 목록 맨 아래
  줄에서 적고 Enter 를 누릅니다. 달력에서 날짜를 고르면 그 날이 기한이 됩니다.
- **종료된 사업은 접힙니다.** 종료일이 지나면 목록에서 접어 두고, 눌러서
  펼쳐 볼 수 있습니다. 지우는 것이 아니라 접는 것이라 자료는 그대로입니다.
- 예시 자료를 넣고 지우는 방법을 [docs/개발.md](docs/개발.md) 에 적었습니다.

### 달라진 것

- **금액을 모두 원 단위로 씁니다.** 전에는 화면에만 억으로 줄여 적어
  실제 금액을 바로 읽을 수 없었습니다. 입력도 표시도 원으로 통일했습니다.
- **공고는 공고일 최신순으로** 먼저 보입니다. (전에는 마감 임박순)
- 전체 사업 현황에서 **일정과 사업 목록을 한 줄에** 둡니다.
- 사업 대시보드에서 **달력이 위로** 올라오고, 진행 단계 카드는 뺐습니다.
- **사업 구분 색**을 색상이 서로 먼 여덟 색으로 바꿨습니다. 적록색맹인 분께도
  구분되는 조합입니다. 전에는 다섯 색뿐이라 여섯 번째 사업부터 색이 겹쳤습니다.
- 전체 현황의 합계(총 사업비·총 집행액)는 **진행 중인 사업만** 셉니다.
- 페이지 제목을 키우고, 달력의 '크게 보기' 를 아이콘으로 바꿨습니다.
- 공고 상태줄에서 기관 서버 사정을 적던 문구를 빼고 마지막 수집 시각과
  건수만 남겼습니다.
- README 를 쓰는 사람 기준으로 다시 쓰고, 개발 내용은 `docs/개발.md` 로
  옮겼습니다.

### 고친 것

- **한글 Windows 에서 `alembic` 이 아예 돌지 않던 문제.** 설정 파일에 한글
  주석이 있었는데 alembic 이 이 파일을 로케일(cp949)로 읽어 오류가 났습니다.
  서버에서 표 구조를 바꿀 수 없는 상태였습니다.
- 회차 입력창이 화면보다 길면 입력창과 대시보드가 번갈아 스크롤되던 문제.
- Windows 에서 개발 서버·검증 스크립트가 돌지 않던 문제(경로·인코딩).
- 예시 자료 스크립트가 운영 데이터베이스에서도 그냥 돌던 문제.
  이제 확인 문장을 입력해야 진행합니다.

### 없앤 것

- **공고 예약 수집.** 한 번도 돈 적이 없는 기능이었습니다. 수집은 화면의
  [지금 수집] 버튼으로만 합니다.

### 안에서 바뀐 것

- 운영 데이터베이스를 PostgreSQL 로 정했습니다(개발 PC 는 SQLite 그대로).
- 화면 빌드 결과물을 저장소에 함께 넣어, 서버가 `git pull` 만으로 화면까지
  받습니다. 서버에 Node.js 가 필요 없습니다.
- `requirements.txt` 를 저장소 맨 위로 옮기고 안 쓰는 것을 뺐습니다.
- 서버에 올릴 준비를 한 번에 하는 `scripts/release.ps1` · `release.sh` 를
  넣었습니다. 테스트 · 시안 대조 · 화면 빌드를 묶어서 합니다.
- 자동 시험 73건 → 75건.

---

## 0.1.0

첫 판. 사업 진척·예산 집행 기록, 기관 네 곳 공고 수집, 대시보드.
