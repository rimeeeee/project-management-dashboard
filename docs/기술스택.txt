사업관리 대시보드 — 기술 스택


■ 백엔드 — Python 3.13

  FastAPI            0.141.1    웹 프레임워크
  Uvicorn            0.52.4     웹 서버
  SQLAlchemy         2.0.52     DB 접근 (ORM)
  Alembic            1.19.1     DB 스키마 변경 관리
  Pydantic           2.13.4     입력값 검증
  psycopg            3.3.4      PostgreSQL 드라이버
  APScheduler        3.11.3     공고 자동 수집 스케줄
  argon2-cffi        25.1.0     비밀번호 해싱
  itsdangerous       2.2.0      세션 쿠키 서명


■ 프론트엔드 — Node 24

  React              18.3.1
  TypeScript         5.9.3
  Vite               5.4.21     빌드 도구

  상태 관리·UI 라이브러리 없음.
  CSS 가 프로토타입에서 그대로 복사한 것이라 Tailwind 등을 섞으면 화면이 틀어짐.
  빌드 결과: JS 210KB (gzip 66KB) + CSS 239KB (gzip 63KB)


■ 데이터베이스

  PostgreSQL 16   배포용
  SQLite          로컬 개발용

  같은 코드가 둘 다 지원. DATABASE_URL 만 바꾸면 전환. 표 14개.


■ 공고 수집

  외부 라이브러리 없이 파이썬 표준 기능만 사용.
  수집처: 보건산업진흥원 · 보건복지인재원 · 보건의료정보원 · NTIS


■ 로그인

  비밀번호 한 개 (담당자 계정 없음). Argon2 해시, 서명된 세션 쿠키.


────────────────────────────────────────────────
배포 전 반드시 바꿀 것

  SECRET_KEY           지금 기본값 — 안 바꾸면 세션 쿠키 위조 가능
  APP_PASSWORD_HASH    지금 비밀번호가 '2026'
  COOKIE_SECURE=true   https 로 서비스한다면


전산실 확인 사항

  1. 어디에 올릴 수 있는지 (내부 서버 / 클라우드)
  2. 서버에서 외부 인터넷에 나갈 수 있는지  ★ 안 되면 공고 수집 불가
  3. 고정 IP 가 있는지


※ PostgreSQL 로는 아직 실행해 보지 못했습니다 (개발 PC 에 Docker 없음).
   서버가 정해지면 이것부터 확인해야 합니다.
