# 폴더 구조 문서 다시 만들기

`docs/폴더구조.txt` 는 손으로 적은 것이 아니라 실제 폴더를 읽어 만든 것입니다.
파일을 추가하거나 옮긴 뒤에는 다시 돌려 주세요.

```bash
.venv/bin/python scripts/docs/폴더구조_만들기.py
```

폴더에 설명을 붙이려면 `폴더구조_만들기.py` 의 `NOTE` 에 한 줄 추가하면 됩니다.

(`docs/기술스택.txt` 는 짧아서 직접 고칩니다.)
